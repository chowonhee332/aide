# file-explorer - Hierarchical browser that drills through nested levels as adjacent columns rather than replacing the list each time, with breadcrumb history, switchable list and grid density, and a metadata pane for the selected node. Files, folders, directory, tree, or Finder-style navigator.
# Components: Avatar, IconButton, Layout, List, MetadataList, SegmentedControl, Toolbar

<Layout height="fill">
  /* header: */
  <Toolbar>
    <IconButton variant="ghost" />
    <IconButton variant="ghost" />
    <Text />
    <SegmentedControl />
    <SegmentedControlItem />
    <SegmentedControlItem />
    <SegmentedControlItem />
    <SegmentedControlItem />
    <IconButton variant="ghost" />
    <IconButton variant="ghost" />
    <IconButton variant="ghost" />
    <IconButton variant="ghost" />
    <IconButton variant="ghost" />
    /* content: */
    <LayoutContent padding={0}>
      <HStack height="100%">
        <Section padding={2} variant="transparent" width={240}>
          <List density="compact">
            <ListItem />
            <Icon />
            <Icon />
          </List>
        </Section>
        <Section padding={6} variant="transparent">
          <VStack gap={4}>
            <Avatar />
            <VStack gap={1}>
              <Text />
              <Text />
            </VStack>
            <MetadataList />
            <MetadataListItem />
            ...
