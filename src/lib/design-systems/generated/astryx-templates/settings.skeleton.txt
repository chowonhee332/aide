# settings - Preferences as one continuously scrolling form, sections stacked in order with no navigation between them. The simplest settings shape - everything reachable by scrolling. Settings, preferences, configuration, options, or account.
# Components: CheckboxInput, Divider, Grid, Layout, List, Tab, TabList, TextInput, Typeahead

<Layout hasDivider height="fill">
  /* header: */
  <LayoutHeader hasDivider>
    <StackItem />
    <Heading />
    <Typeahead />
  </LayoutHeader>
  /* start: */
  <LayoutPanel padding={2} hasDivider={false} width={260}>
    <List density="balanced">
      <ListItem />
    </List>
  </LayoutPanel>
  /* content: */
  <LayoutContent padding={4}>
    <VStack gap={4}>
      <TabList>
        <Tab />
      </TabList>
    </VStack>
    <Grid gap={10} columns={{minWidth: 320}}>
      <VStack gap={1}>
        <Heading />
        <Text />
      </VStack>
      <VStack gap={4}>
        <TextInput />
        <TextInput />
        <TextInput />
        <TextInput />
        <Button variant="primary" />
      </VStack>
    </Grid>
    <Divider />
    <Grid gap={10} columns={{minWidth: 320}}>
      ...
