# product-gallery - Uniform card grid where every tile carries media, title, supporting line, and a price. Browse-to-select only - no filtering, no detail surface. Products, catalog, storefront, shop, listings, or ecommerce grid.
# Components: AspectRatio, Card, Grid, Layout, ProductCard

<Layout padding={6} height="fill">
  /* content: */
  <LayoutContent padding={6}>
    <VStack gap={6}>
      <Grid gap={4} columns={{minWidth: 280}}>
        <Heading />
        <VStack gap={3}>
          <Text />
          <Button variant="primary" />
        </VStack>
      </Grid>
      <Grid gap={6} columns={{minWidth: 300}}>
        <ProductCard />
      </Grid>
    </VStack>
  </LayoutContent>
