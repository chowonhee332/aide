# editor - Three-pane authoring workspace: a source palette on one flank, a live canvas in the middle, and an inspector that retargets to whatever is selected. Drag and drop to reorder, edit to see it render immediately. Builder, composer, drag and drop, page builder, or WYSIWYG editor.
# Components: BlockPreview, Card, Center, Dialog, Divider, EmptyState, Layout, List, PropertiesForm, SegmentedControl, Selector, Spinner, Tab, TabList, Table, TextArea, TextInput, Toolbar

<Layout padding={8} height="fill">
  /* header: */
  /* start: */
  /* content: */
  <LayoutContent padding={8}>
    <VStack gap={4}>
      <BlockPreview />
      <EmptyState />
    </VStack>
  </LayoutContent>
  <Dialog padding={0} variant="fullscreen">
    <Layout hasDivider height="fill">
      /* header: */
      <LayoutHeader hasDivider>
        <HStack gap={3}>
          <Heading />
          <Button />
        </HStack>
      </LayoutHeader>
      /* content: */
