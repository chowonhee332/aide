import { cn } from "@/lib/utils"
function Progress({value=0,className,label}: {value?:number;className?:string;label?:string}){const safe=Math.max(0,Math.min(100,value));return <div className={cn("grid gap-1.5",className)}>{label&&<div className="flex justify-between text-xs text-[var(--aui-text-muted)]"><span>{label}</span><span>{safe}%</span></div>}<div role="progressbar" aria-label={label} aria-valuemin={0} aria-valuemax={100} aria-valuenow={safe} className="h-[var(--aui-component-feedback-progress-height)] overflow-hidden rounded-[var(--aui-radius-pill)] bg-[var(--aui-fill)]"><div className="h-full rounded-[var(--aui-radius-pill)] bg-[var(--aui-primary)] transition-[width] duration-[var(--aui-motion-base)]" style={{width:`${safe}%`}}/></div></div>}
function Skeleton({className,...props}:React.ComponentProps<"div">){return <div aria-hidden data-slot="skeleton" className={cn("h-3 animate-pulse rounded bg-[var(--aui-fill)] motion-reduce:animate-none",className)} {...props}/>}
export { Progress, Skeleton }
